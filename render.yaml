services:
  - type: web
    name: tomobilti-platform
    env: node
    plan: starter  # Updated to match your $7/month plan
    buildCommand: npm ci && npm run build
    startCommand: npm start
    nodeVersion: "20"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: tomobilti-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: SESSION_SECRET
        generateValue: true
      - key: CSRF_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://tomobilti-platform.onrender.com
    healthCheckPath: /api/cars

databases:
  - name: tomobilti-db
    plan: starter  # Updated to match your $7/month plan
    databaseName: tomobilti
    user: tomobilti_user

